<h2 class="img-banner-text text-light p-3 p-md-5 mt-5 ml-3 ml-md-5 col-10 col-md-6">
  {{#if link}}
    {{#link-to link}}
      {{title}} <br> <span class="subtitle"> {{subtitle}} </span>
    {{/link-to}}
  {{else}}
    {{title}} <br> <span class="subtitle"> {{subtitle}} </span>
  {{/if}}
</h2>

<img class="img-banner-img" alt="title" src={{imageSrc}}>
