<div class="container">
  <div class="col-12 mt-5">
    <h1> Partners </h1>
    <p>
      Without our sponsors, the RoboTeam Twente would not have been where it is right now. Various companies and other parties support us with money, materials and advice.
      Not only do we have branding to give in return, we also visit our partners with out robots, giving demonstrations or interactive workshops. This way we can 
      also inspire even more people about robotics and artificial intelligence.
    </p>

    {{#if gigaByteSponsors}}
      <h3 class="mt-5"> Gigabyte </h3>
      <div class="row">
        {{#each gigaByteSponsors as |sponsor|}}
          <a class="col-4 col-md-3" href={{sponsor.link}}>
            <img class="sponsor" alt={{sponsor.name}} src={{sponsor.imageSrc}}>
          </a>
        {{/each}}
      </div>
    {{/if}}


    {{#if megaByteSponsors}}
      <h3 class="mt-5"> Megabyte </h3>
      <div class="row">
        {{#each megaByteSponsors as |sponsor|}}
          <a class="col-4 col-md-2" href={{sponsor.link}}>
            <img class="sponsor" alt={{sponsor.name}} src={{sponsor.imageSrc}}>
          </a>
        {{/each}}
      </div>
    {{/if}}


    {{#if kiloByteSponsors}}
      <h3 class="mt-5"> KiloByte </h3>
      <div class="row">
        {{#each kiloByteSponsors as |sponsor|}}
          <a class="col-4 {{if sponsor.displayWide 'col-md-3' 'col-md-2'}}" href={{sponsor.link}}>
            <img class="sponsor" alt={{sponsor.name}} src={{sponsor.imageSrc}}>
          </a>
        {{/each}}
      </div>
    {{/if}}

    {{#if byteSponsors}}
      <h3 class="mt-5"> Byte </h3>
      <div class="row">
        {{#each byteSponsors as |sponsor|}}
          <a class="col-4 {{if sponsor.displayWide 'col-md-2' 'col-md-1'}}" href={{sponsor.link}}>
            <img class="sponsor" alt={{sponsor.name}} src={{sponsor.imageSrc}}>
          </a>
        {{/each}}
      </div>
    {{/if}}

    {{#if demoByteSponsors}}
      <h3 class="mt-5"> Byte </h3>
      <div class="row">
        {{#each demoByteSponsors as |sponsor|}}
          <a class="col-4 {{if sponsor.displayWide 'col-md-2' 'col-md-1'}}" href={{sponsor.link}}>
            <img class="sponsor" alt={{sponsor.name}} src={{sponsor.imageSrc}}>
          </a>
        {{/each}}
      </div>
    {{/if}}

    <h3 class="mt-5"> Crowdfunders </h3>
    <div class="row">
      {{#each model.crowdfunders as |crowdfunder|}}
        <div class="col-6"> 
          <span class="text-centered"> {{crowdfunder.name}} </span>
        </div>
      {{/each}}
    </div>
  </div>
</div>




    
